import { Wallet } from '@ethersproject/wallet';
import { BigNumber } from '@ethersproject/bignumber';
import { ContractTransaction } from '@ethersproject/contracts';
export declare type ZoraConfiguredAddresses = {
    media: string;
    market: string;
    currency: string;
};
export declare function setupZora(wallet: Wallet, testWallets: Array<Wallet>): Promise<ZoraConfiguredAddresses>;
export declare function mintCurrency(wallet: Wallet, tokenAdress: string, to: string, amount: BigNumber): Promise<ContractTransaction>;
export declare function approveCurrency(wallet: Wallet, tokenAddress: string, to: string): Promise<ContractTransaction>;
export declare function deployCurrency(wallet: Wallet, name: string, symbol: string, decimals: number): Promise<string>;
