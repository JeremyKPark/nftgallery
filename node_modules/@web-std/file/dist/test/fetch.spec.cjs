'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var lib = require('@web-std/file');
var test$1 = require('./test.cjs');
var fetch = require('@web-std/fetch');

/**
 * @param {import('./test').Test} test
 */
const test = test => {
  test("node-fetch recognizes blobs", async () => {
    // @ts-expect-error - our blob isn't one expected by node-fetch
    const response = new fetch.Response(new lib.File(["hello"], "path/file.txt"));

    test$1.assert.equal(await response.text(), "hello");
  });
};

exports.test = test;
//# sourceMappingURL=fetch.spec.cjs.map
